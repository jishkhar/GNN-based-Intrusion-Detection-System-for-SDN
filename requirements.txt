# Pinned to the versions the Phase 2 results were produced with (Python 3.14).
# torch is the CUDA 13 build on the reference machine; pick the wheel matching
# your platform from https://pytorch.org/get-started/locally/ if needed.
pandas==3.0.2
numpy==2.4.4
scikit-learn==1.8.0
xgboost==3.2.0
networkx==3.6.1
matplotlib==3.10.9
seaborn==0.13.2
tqdm==4.67.3
pyyaml==6.0.3
torch==2.11.0
torch-geometric==2.7.0
requests==2.33.1
pytest==9.1.1
